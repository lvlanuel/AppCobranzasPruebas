<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#3498DB</item>
        <item name="colorPrimaryDark">#3498DB</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="boton_azul">

        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <!--
        <item name="android:layout_margin">@dimen/margen</item>
        <item name="android:padding">@dimen/padding</item>    -->
        <item name="android:background">#3498DB</item>
        <item name="android:text">#ffffff</item>

    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="EstiloListView" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:textColor">#FF0000</item>
        <item name="android:typeface">sans</item>
    </style>

</resources>
